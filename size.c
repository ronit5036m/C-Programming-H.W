#include<stdio.h>
int main(){
    char name;
    int age;
    float sgpa;
    double bmi;

    printf("Size of char = %d\n",sizeof(name));
    printf("Size of int = %d\n",sizeof(age));
    printf("Size of float = %d\n",sizeof(sgpa));
    printf("Size of double = %d\n",sizeof(bmi));
    return 0;
}